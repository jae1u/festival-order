DOMAIN=localhost

DB_HOST=db
DB_USER=root
DB_PASS=rootpassword
DB_NAME=order_db

TZ=Asia/Seoul

# Required. Generate a unique value, for example:
# python3 -c "import secrets; print(secrets.token_urlsafe(48))"
FLASK_SECRET_KEY=

# Required. Do not use admin_password or defaultadmin.
ADMIN_PASSWORD=
